import React from "react";
import { Button } from "@material-ui/core";

function Donations(props) {
  // TODO: Finish this component so that it's fairly similar to the State one.
  /**
   *  I don't want a fancy UI right now cause I want this to be deployed as fast as possible
   *  and I work all day so I don't wanna stretch myself too thin. I want it neat and easy to use and that's it.
   */ 
  
  return (
    <Button>
      {props.name}
    </Button>
  )
}